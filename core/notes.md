# NOTES

### -> i want to have a distrubuted database where the rag will access it without sharing the acutal data i,e ensuring data privarcy.
### -> how will i change the database into vector database since i am building a rag agent that works on top of them so 
### -> how should i convert the existing database into vector database , and the rag should be able to access it so in short a software that will do 

## 1. this is a searching system
## 2. it should be able to take muliple databases before configuring i mean is should be on top of these databases